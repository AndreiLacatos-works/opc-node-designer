import 'package:flutter/widgets.dart';

class DesignerChart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        child: const Text("Designer chart"),
        height: double.infinity,
      ),
    );
  }
}
